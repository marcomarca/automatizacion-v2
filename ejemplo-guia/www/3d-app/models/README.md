# 3D GLB Models Directory

Place your 3D models here:
- `showroom.glb`
- `lobby.glb`

The application will automatically load these files using React Three Fiber.
If a file is missing or still in development, the application falls back smoothly to the procedural 3D digital twin room representation.
